version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    command: gunicorn app:app --bind 0.0.0.0:8080 --timeout 120 --workers 1 --reload
